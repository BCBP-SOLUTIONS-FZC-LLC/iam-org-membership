{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "MFAReset",
  "title": "MFAReset",
  "description": "P-34 (§16 OQ-8/F6) audit signal — emitted after RealmProvisionerClient.ResetMFA (RP-9) succeeds. O&M persists no MFA state of its own; this event exists solely for the Audit Log consumer to record the actor and target of the reset, per RP's confirmed HLD §8.2.6 flow.",
  "type": "object",
  "required": ["tenant_id", "user_id", "actor_id"],
  "properties": {
    "tenant_id": { "type": "string", "format": "uuid" },
    "user_id":   { "type": "string", "format": "uuid" },
    "actor_id":  { "type": "string", "format": "uuid" }
  },
  "additionalProperties": false
}
